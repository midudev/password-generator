import React from 'react'
import styles from '@components/devenapuros/styles/section.module.css'

export const MainSection = ({ children }) => {
	return <section className={styles.mainSection}>{children}</section>
}
